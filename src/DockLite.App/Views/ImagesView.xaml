<UserControl x:Class="DockLite.App.Views.ImagesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:DockLite.App.Converters"
             xmlns:behaviors="clr-namespace:DockLite.App.Behaviors"
             d:DesignHeight="560"
             d:DesignWidth="720"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:InvertBooleanConverter x:Key="InvertBooleanConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{DynamicResource Ui_Images_PageTitle}"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource ThemePageTitleBrush}"
                   Margin="0,0,0,16" />

        <Grid Grid.Row="1"
              Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="160" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="180" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0"
                       Text="{DynamicResource Ui_Images_FilterLabel}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource ThemeBodyTextBrush}"
                       Margin="0,0,8,0" />
            <ComboBox Grid.Column="1"
                      VerticalAlignment="Center"
                      DisplayMemberPath="Label"
                      ItemsSource="{Binding ImageFilterKindOptions}"
                      SelectedItem="{Binding SelectedImageFilterKindOption}" />
            <TextBlock Grid.Column="3"
                       Text="{DynamicResource Ui_Images_SearchScopeLabel}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource ThemeBodyTextBrush}"
                       Margin="0,0,8,0" />
            <ComboBox Grid.Column="4"
                      VerticalAlignment="Center"
                      DisplayMemberPath="Label"
                      ItemsSource="{Binding ImageSearchScopeOptions}"
                      SelectedItem="{Binding SelectedImageSearchScopeOption}" />
            <TextBlock Grid.Column="6"
                       Text="{DynamicResource Ui_Images_SearchLabel}"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource ThemeBodyTextBrush}"
                       Margin="0,0,8,0" />
            <TextBox x:Name="PrimarySearchTextBox"
                     Grid.Column="7"
                     Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                     Padding="6,4"
                     Margin="0,0,8,0"
                     ToolTip="{DynamicResource Ui_Images_SearchTextTooltip}" />
            <Button Grid.Column="8"
                    Content="{DynamicResource Ui_Images_RefreshButton}"
                    Padding="12,6"
                    MinWidth="100"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBooleanConverter}}" />
        </Grid>

        <Border Grid.Row="2"
                Margin="0,0,0,10"
                Padding="10,8"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <StackPanel>
                <TextBlock FontSize="12"
                           Foreground="{DynamicResource ThemeBodyTextBrush}"
                           TextWrapping="Wrap"
                           Text="{Binding ImageListSummaryText}" />
                <TextBlock Margin="0,6,0,0"
                           FontSize="12"
                           Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                           TextWrapping="Wrap"
                           Text="{Binding ImagePruneHintText}" />
            </StackPanel>
        </Border>

        <Grid Grid.Row="3"
              MinHeight="300"
              Margin="0,0,0,8">
            <Border behaviors:MouseWheelScrollBubbling.BubbleToMainContentScroll="True"
                    Style="{StaticResource RoundedTableChromeStyle}">
            <DataGrid ItemsSource="{Binding FilteredItems}"
                      SelectedItem="{Binding SelectedImage}"
                      AutoGenerateColumns="False"
                      IsReadOnly="False"
                      CanUserAddRows="False"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling">
                <DataGrid.Columns>
                    <DataGridTemplateColumn Width="48"
                                            Header="{DynamicResource Ui_Images_Grid_Select}">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox HorizontalAlignment="Center"
                                          IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Width="*"
                                        Binding="{Binding Model.Repository}"
                                        Header="{DynamicResource Ui_Images_Grid_Repository}"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="ToolTip" Value="{Binding Model.Repository}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="100"
                                        Binding="{Binding Model.Tag}"
                                        Header="{DynamicResource Ui_Images_Grid_Tag}"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="ToolTip" Value="{Binding Model.Tag}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="160"
                                        Binding="{Binding Model.Id}"
                                        Header="{DynamicResource Ui_Images_Grid_Id}"
                                        IsReadOnly="True">
                        <DataGridTextColumn.ElementStyle>
                            <Style TargetType="TextBlock">
                                <Setter Property="ToolTip" Value="{Binding Model.Id}" />
                            </Style>
                        </DataGridTextColumn.ElementStyle>
                    </DataGridTextColumn>
                    <DataGridTextColumn Width="90"
                                        Binding="{Binding Model.Size}"
                                        Header="{DynamicResource Ui_Images_Grid_Size}"
                                        IsReadOnly="True" />
                    <DataGridTextColumn Width="140"
                                        Binding="{Binding Model.CreatedAt}"
                                        Header="{DynamicResource Ui_Images_Grid_Created}"
                                        IsReadOnly="True" />
                </DataGrid.Columns>
            </DataGrid>
            </Border>
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="420"
                       Margin="16"
                       Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                       FontSize="13"
                       IsHitTestVisible="False"
                       Text="{DynamicResource Ui_Images_EmptyListHint}"
                       Visibility="{Binding ShowEmptyImageListHint, Converter={StaticResource BoolToVisibilityConverter}}" />
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="420"
                       Margin="16"
                       Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                       FontSize="13"
                       IsHitTestVisible="False"
                       Text="{DynamicResource Ui_Images_FilterEmptyHint}"
                       Visibility="{Binding ShowImageFilterEmptyHint, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>

        <Expander Grid.Row="4"
                  Margin="0,0,0,10"
                  Foreground="{DynamicResource ThemeBodyTextBrush}"
                  Header="Chi tiết image (inspect, history, pull, export/import)"
                  IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBooleanConverter}}">
            <ScrollViewer MaxHeight="520"
                          behaviors:MouseWheelScrollBubbling.BubbleToMainContentScroll="True"
                          VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="0,8,0,0">
                    <StackPanel Orientation="Horizontal"
                                Margin="0,0,0,8">
                        <Button Margin="0,0,8,4"
                                Padding="10,6"
                                Content="Tải inspect (JSON)"
                                Command="{Binding LoadInspectCommand}"
                                IsEnabled="{Binding IsDetailBusy, Converter={StaticResource InvertBooleanConverter}}" />
                        <Button Margin="0,0,8,4"
                                Padding="10,6"
                                Content="Tải history (layer)"
                                Command="{Binding LoadHistoryCommand}"
                                IsEnabled="{Binding IsDetailBusy, Converter={StaticResource InvertBooleanConverter}}" />
                    </StackPanel>
                    <TextBlock FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeBodyTextBrush}"
                               Text="Inspect" />
                    <TextBox MinHeight="120"
                             Margin="0,4,0,8"
                             Padding="8"
                             FontFamily="Consolas"
                             FontSize="11"
                             IsReadOnly="True"
                             Text="{Binding DetailInspectText}"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" />
                    <TextBlock FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeBodyTextBrush}"
                               Text="History (layer)" />
                    <DataGrid MaxHeight="200"
                              Margin="0,4,0,8"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              HeadersVisibility="Column"
                              IsReadOnly="True"
                              ItemsSource="{Binding HistoryRows}">
                        <DataGrid.Columns>
                            <DataGridTextColumn Width="120"
                                                Binding="{Binding Id}"
                                                Header="Id" />
                            <DataGridTextColumn Width="140"
                                                Binding="{Binding CreatedText}"
                                                Header="Tạo" />
                            <DataGridTextColumn Width="*"
                                                MinWidth="160"
                                                Binding="{Binding CreatedBy}"
                                                Header="CreatedBy" />
                            <DataGridTextColumn Width="80"
                                                Binding="{Binding SizeText}"
                                                Header="Size" />
                            <DataGridTextColumn Width="120"
                                                Binding="{Binding Comment}"
                                                Header="Comment" />
                        </DataGrid.Columns>
                    </DataGrid>
                    <TextBlock FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeBodyTextBrush}"
                               Text="Pull image (docker pull)" />
                    <TextBlock Margin="0,4,0,4"
                               Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Text="Nhập reference đầy đủ (ví dụ nginx:latest). Log từ daemon có thể rút gọn." />
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Column="0"
                                 Margin="0,0,8,0"
                                 Text="{Binding PullReferenceText, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding IsDetailBusy, Converter={StaticResource InvertBooleanConverter}}" />
                        <Button Grid.Column="1"
                                Padding="12,6"
                                Content="Pull"
                                Command="{Binding PullImageCommand}"
                                IsEnabled="{Binding IsDetailBusy, Converter={StaticResource InvertBooleanConverter}}" />
                    </Grid>
                    <ProgressBar Height="4"
                                 Margin="0,4,0,4"
                                 IsIndeterminate="True"
                                 Visibility="{Binding IsPullInProgress, Converter={StaticResource BoolToVisibilityConverter}}" />
                    <TextBlock Margin="0,0,0,4"
                               FontSize="11"
                               Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                               Text="Log pull" />
                    <TextBox MinHeight="80"
                             Margin="0,0,0,8"
                             Padding="8"
                             FontFamily="Consolas"
                             FontSize="11"
                             IsReadOnly="True"
                             Text="{Binding PullLogText}"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" />
                    <TextBlock FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeBodyTextBrush}"
                               Text="{DynamicResource Ui_Images_TrivyTitle}" />
                    <TextBlock Margin="0,4,0,8"
                               Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Text="{DynamicResource Ui_Images_TrivyHint}" />
                    <Grid Margin="0,0,0,8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Grid.Row="0"
                                 Grid.Column="0"
                                 Margin="0,0,8,8"
                                 Padding="6,4"
                                 Text="{Binding TrivyImageRef, UpdateSourceTrigger=PropertyChanged}"
                                 IsEnabled="{Binding IsTrivyBusy, Converter={StaticResource InvertBooleanConverter}}" />
                        <Button Grid.Row="0"
                                Grid.Column="1"
                                Margin="0,0,0,8"
                                Padding="12,6"
                                VerticalAlignment="Top"
                                Content="{DynamicResource Ui_Images_TrivyScanButton}"
                                Command="{Binding ScanTrivyCommand}"
                                IsEnabled="{Binding IsTrivyBusy, Converter={StaticResource InvertBooleanConverter}}" />
                        <Grid Grid.Row="1"
                              Grid.Column="0"
                              Grid.ColumnSpan="2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="140" />
                                <ColumnDefinition Width="16" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"
                                       Foreground="{DynamicResource ThemeBodyTextBrush}"
                                       Text="{DynamicResource Ui_Images_Trivy_FormatLabel}" />
                            <ComboBox Grid.Column="1"
                                      VerticalAlignment="Center"
                                      DisplayMemberPath="Label"
                                      ItemsSource="{Binding TrivyFormatOptions}"
                                      SelectedItem="{Binding SelectedTrivyFormatOption}"
                                      IsEnabled="{Binding IsTrivyBusy, Converter={StaticResource InvertBooleanConverter}}" />
                            <TextBlock Grid.Column="3"
                                       VerticalAlignment="Center"
                                       Margin="0,0,8,0"
                                       Foreground="{DynamicResource ThemeBodyTextBrush}"
                                       Text="{DynamicResource Ui_Images_Trivy_PolicyPathLabel}" />
                            <TextBox Grid.Column="4"
                                     Padding="6,4"
                                     ToolTip="{DynamicResource Ui_Images_Trivy_PolicyPathTooltip}"
                                     Text="{Binding TrivyPolicyPath, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding IsTrivyBusy, Converter={StaticResource InvertBooleanConverter}}" />
                        </Grid>
                    </Grid>
                    <TextBox MinHeight="100"
                             Margin="0,0,0,8"
                             Padding="8"
                             FontFamily="Consolas"
                             FontSize="11"
                             IsReadOnly="True"
                             Text="{Binding TrivyOutputText}"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto" />
                    <TextBlock FontWeight="SemiBold"
                               Foreground="{DynamicResource ThemeBodyTextBrush}"
                               Text="Export / import (docker save / load)" />
                    <TextBlock Margin="0,4,0,8"
                               Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                               FontSize="12"
                               TextWrapping="Wrap"
                               Text="Xuất: cần chọn một dòng image. Nhập: chọn file .tar từ docker save; có thể mất vài phút với image lớn." />
                    <StackPanel Orientation="Horizontal">
                        <Button Margin="0,0,8,4"
                                Padding="12,6"
                                Content="Xuất tar (image đang chọn)"
                                Command="{Binding ExportImageTarCommand}"
                                IsEnabled="{Binding IsDetailBusy, Converter={StaticResource InvertBooleanConverter}}" />
                        <Button Margin="0,0,8,4"
                                Padding="12,6"
                                Content="Nhập tar…"
                                Command="{Binding ImportImageTarCommand}"
                                IsEnabled="{Binding IsDetailBusy, Converter={StaticResource InvertBooleanConverter}}" />
                    </StackPanel>
                </StackPanel>
            </ScrollViewer>
        </Expander>

        <WrapPanel Grid.Row="5"
                   Margin="0,10,0,8">
            <Button Content="Chọn tất cả"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    Command="{Binding SelectAllFilteredCommand}"
                    IsEnabled="{Binding CanSelectAllFiltered}" />
            <Button Content="Bỏ chọn"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    Command="{Binding ClearRowSelectionChecksCommand}"
                    IsEnabled="{Binding CanClearSelection}" />
            <Button Content="{DynamicResource Ui_Images_BatchCopyIds}"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    MinWidth="120"
                    Command="{Binding CopySelectedIdsToClipboardCommand}"
                    IsEnabled="{Binding CanCopySelectedIds}" />
            <Button Content="Xóa các dòng đã chọn"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    Command="{Binding BatchRemoveCheckedCommand}"
                    IsEnabled="{Binding CanBatchRemove}" />
            <Button Content="Xóa image (dòng highlight)"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    Command="{Binding RemoveSelectedCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBooleanConverter}}" />
            <Button Content="Prune dangling"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    Command="{Binding PruneDanglingCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBooleanConverter}}" />
            <Button Content="Prune mọi image không dùng (-a)"
                    Margin="0,0,8,4"
                    Padding="12,6"
                    Command="{Binding PruneAllUnusedCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBooleanConverter}}" />
        </WrapPanel>

        <Border Grid.Row="6"
                BorderBrush="{DynamicResource ThemeSeparatorBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,8,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource ThemeBodyTextBrush}" />
        </Border>
    </Grid>
</UserControl>
