<UserControl x:Class="DockLite.App.Views.DockerEventsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:DockLite.App.Converters"
             d:DesignHeight="520"
             d:DesignWidth="720"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:InvertBooleanConverter x:Key="InvertBooleanConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="{DynamicResource Ui_DockerEvents_PageTitle}"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource ThemePageTitleBrush}"
                   Margin="0,0,0,12" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,8">
            <Button Content="{DynamicResource Ui_DockerEvents_Start}"
                    Padding="12,6"
                    MinWidth="100"
                    Margin="0,0,8,0"
                    Command="{Binding StartStreamingCommand}"
                    IsEnabled="{Binding IsStreaming, Converter={StaticResource InvertBooleanConverter}}" />
            <Button Content="{DynamicResource Ui_DockerEvents_Stop}"
                    Padding="12,6"
                    MinWidth="100"
                    Margin="0,0,8,0"
                    Command="{Binding StopStreamingCommand}"
                    IsEnabled="{Binding IsStreaming}" />
            <Button Content="{DynamicResource Ui_DockerEvents_Clear}"
                    Padding="12,6"
                    MinWidth="88"
                    Command="{Binding ClearEventsCommand}" />
        </StackPanel>

        <TextBlock Grid.Row="2"
                   Margin="0,0,0,10"
                   FontSize="12"
                   Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                   TextWrapping="Wrap"
                   Text="{DynamicResource Ui_DockerEvents_Hint}" />

        <Border Grid.Row="3"
                Padding="10"
                Background="{DynamicResource CardBackgroundBrush}"
                BorderBrush="{DynamicResource CardBorderBrush}"
                BorderThickness="1"
                CornerRadius="8">
            <DockPanel LastChildFill="True">
                <TextBlock DockPanel.Dock="Top"
                           Margin="0,0,0,8"
                           FontSize="12"
                           Foreground="{DynamicResource ThemeBodyTextBrush}"
                           TextWrapping="Wrap"
                           Text="{Binding StatusMessage}" />
                <ScrollViewer VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <TextBox Text="{Binding EventsText, Mode=OneWay}"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             FontSize="11"
                             BorderThickness="0"
                             Background="Transparent"
                             Foreground="{DynamicResource ThemeBodyTextBrush}" />
                </ScrollViewer>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
