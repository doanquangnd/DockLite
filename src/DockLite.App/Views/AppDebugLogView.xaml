<UserControl x:Class="DockLite.App.Views.AppDebugLogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DesignHeight="520"
             d:DesignWidth="720"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Nhật ký ứng dụng"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,8" />
        <TextBlock Grid.Row="1"
                   TextWrapping="Wrap"
                   Margin="0,0,0,12"
                   Foreground="#444">
            File log theo ngày trong thư mục bên dưới (UTF-8). Dùng khi gỡ lỗi kết nối WSL, lưu cài đặt, lỗi UI.
        </TextBlock>

        <TextBox Grid.Row="2"
                 Text="{Binding LogFolderPath, Mode=OneWay}"
                 IsReadOnly="True"
                 Padding="8"
                 Margin="0,0,0,12"
                 FontFamily="Consolas"
                 FontSize="12" />

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <WrapPanel Grid.Row="0"
                       Margin="0,0,0,10">
                <Button Content="Làm mới"
                        Margin="0,0,8,4"
                        Padding="12,6"
                        MinWidth="100"
                        Command="{Binding RefreshCommand}" />
                <Button Content="Mở thư mục log"
                        Margin="0,0,8,4"
                        Padding="12,6"
                        MinWidth="100"
                        Command="{Binding OpenLogFolderCommand}" />
            </WrapPanel>
            <TextBox Grid.Row="1"
                     Text="{Binding LogText, Mode=OneWay}"
                     IsReadOnly="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     HorizontalScrollBarVisibility="Auto"
                     FontFamily="Consolas"
                     FontSize="12"
                     BorderBrush="#DDD"
                     BorderThickness="1"
                     Padding="8" />
        </Grid>
    </Grid>
</UserControl>
