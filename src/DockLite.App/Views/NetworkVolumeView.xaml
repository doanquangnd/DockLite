<UserControl x:Class="DockLite.App.Views.NetworkVolumeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:converters="clr-namespace:DockLite.App.Converters"
             xmlns:behaviors="clr-namespace:DockLite.App.Behaviors"
             d:DesignHeight="520"
             d:DesignWidth="720"
             mc:Ignorable="d">
    <UserControl.Resources>
        <converters:InvertBooleanConverter x:Key="InvertBooleanConverter" />
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Mạng và volume"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Foreground="{DynamicResource ThemePageTitleBrush}"
                   Margin="0,0,0,16" />

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    Margin="0,0,0,12">
            <Button Content="Làm mới"
                    Padding="12,6"
                    MinWidth="120"
                    Command="{Binding RefreshCommand}"
                    IsEnabled="{Binding IsBusy, Converter={StaticResource InvertBooleanConverter}}" />
            <Button Content="{DynamicResource Ui_NetVol_RemoveVolume}"
                    Padding="12,6"
                    MinWidth="120"
                    Margin="8,0,0,0"
                    Command="{Binding RemoveVolumeCommand}"
                    IsEnabled="{Binding CanRemoveVolume}" />
        </StackPanel>

        <Grid Grid.Row="2"
              MinHeight="160"
              Margin="0,0,0,12">
            <Border behaviors:MouseWheelScrollBubbling.BubbleToMainContentScroll="True"
                    Style="{StaticResource RoundedTableChromeStyle}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource ThemeBodyTextBrush}"
                           Text="Network" />
                <DataGrid ItemsSource="{Binding Networks}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*"
                                            MinWidth="120"
                                            Binding="{Binding Name}"
                                            Header="Tên">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="ToolTip" Value="{Binding Name}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Width="140"
                                            Binding="{Binding Id}"
                                            Header="Id">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="ToolTip" Value="{Binding Id}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Width="100"
                                            Binding="{Binding Driver}"
                                            Header="Driver" />
                        <DataGridTextColumn Width="80"
                                            Binding="{Binding Scope}"
                                            Header="Scope" />
                        <DataGridTextColumn Width="160"
                                            Binding="{Binding CreatedAt}"
                                            Header="Tạo" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
            </Border>
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="400"
                       Margin="16"
                       Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                       FontSize="12"
                       IsHitTestVisible="False"
                       Text="{DynamicResource Ui_NetVol_EmptyNetworksHint}"
                       Visibility="{Binding ShowEmptyNetworksHint, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>

        <Grid Grid.Row="3"
              MinHeight="160"
              Margin="0,0,0,8">
            <Border behaviors:MouseWheelScrollBubbling.BubbleToMainContentScroll="True"
                    Style="{StaticResource RoundedTableChromeStyle}">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top"
                           Margin="0,0,0,8"
                           FontWeight="SemiBold"
                           Foreground="{DynamicResource ThemeBodyTextBrush}"
                           Text="Volume" />
                <DataGrid ItemsSource="{Binding Volumes}"
                          SelectedItem="{Binding SelectedVolume}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          SelectionMode="Single"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          EnableRowVirtualization="True"
                          EnableColumnVirtualization="True"
                          VirtualizingPanel.IsVirtualizing="True"
                          VirtualizingPanel.VirtualizationMode="Recycling">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="*"
                                            MinWidth="140"
                                            Binding="{Binding Name}"
                                            Header="Tên">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="ToolTip" Value="{Binding Name}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Width="100"
                                            Binding="{Binding Driver}"
                                            Header="Driver" />
                        <DataGridTextColumn Width="*"
                                            MinWidth="160"
                                            Binding="{Binding Mountpoint}"
                                            Header="Mountpoint">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="ToolTip" Value="{Binding Mountpoint}" />
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                        <DataGridTextColumn Width="80"
                                            Binding="{Binding Scope}"
                                            Header="Scope" />
                        <DataGridTextColumn Width="160"
                                            Binding="{Binding CreatedAt}"
                                            Header="Tạo" />
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
            </Border>
            <TextBlock HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"
                       MaxWidth="400"
                       Margin="16"
                       Foreground="{DynamicResource ThemeMutedForegroundBrush}"
                       FontSize="12"
                       IsHitTestVisible="False"
                       Text="{DynamicResource Ui_NetVol_EmptyVolumesHint}"
                       Visibility="{Binding ShowEmptyVolumesHint, Converter={StaticResource BoolToVisibilityConverter}}" />
        </Grid>

        <Border Grid.Row="4"
                BorderBrush="{DynamicResource ThemeSeparatorBorderBrush}"
                BorderThickness="0,1,0,0"
                Padding="0,8,0,0">
            <TextBlock Text="{Binding StatusMessage}"
                       TextWrapping="Wrap"
                       Foreground="{DynamicResource ThemeBodyTextBrush}" />
        </Border>
    </Grid>
</UserControl>
