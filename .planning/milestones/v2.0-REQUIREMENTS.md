# Archive: Requirements tại đóng milestone v2.0

**Milestone:** v2.0 IPC Hardening (Zero-Trust)  
**Ngày archive:** 2026-04-25  
**Nguồn:** `.planning/REQUIREMENTS.md` (đã gỡ khỏi working tree sau commit đóng milestone)

---

# Requirements: DockLite

**Defined:** 2026-04-23  
**Core Value:** DockLite phải chạy an toàn mặc định theo Zero-Trust; IPC giữa WPF và Go service là kênh tin cậy ngay cả khi user opt-in mở LAN.

Mỗi requirement v2.0 có **ASVS Control** map tới OWASP ASVS 4.0.3 L2. `SECURITY-ATTESTATION.md` self-attest COVERED / PARTIAL / N/A cho từng mục.

## v2.0 Requirements — IPC Hardening (Zero-Trust)

### Network Surface Reduction (NET)

- [x] **NET-01**: Service Go mặc định bind `127.0.0.1:17890` thay cho `0.0.0.0:17890`. (ASVS V14.1.4, V1.14.3)
- [x] **NET-02**: Service Go fail-closed — từ chối khởi động nếu `DOCKLITE_ADDR` non-loopback VÀ `DOCKLITE_API_TOKEN` trống. Log lý do vào stderr trước khi exit code 2. (ASVS V14.2.1, V14.1.1)
- [x] **NET-03**: WebSocket `CheckOrigin` mặc định chỉ accept `localhost`, `127.0.0.1`, `[::1]`. Env `DOCKLITE_ALLOWED_ORIGINS` mở rộng whitelist khi user opt-in LAN mode. (ASVS V13.2.6, V9.3.1)
- [x] **NET-04**: UI Settings hiển thị warning banner khi `ServiceBaseUrl` non-loopback hoặc scheme không phải HTTPS. Text warning ở light/dark đều đạt contrast AA. (ASVS V14.4.1)
- [x] **NET-05**: `.env.example` và docs cập nhật: mặc định loopback, nêu rõ LAN mode là opt-in và cần token + TLS. (ASVS V14.1.1)

### Secrets at Rest (SEC)

- [x] **SEC-01**: `ServiceApiToken` lưu trong Windows Credential Manager qua `Windows.Security.Credentials.PasswordVault` (resource `DockLite:ServiceApiToken:{profile}`, userName `DockLiteClient`). (ASVS V2.10.4, V6.1.1, V6.1.2)
- [x] **SEC-02**: `settings.json` lưu `ServiceApiTokenProfile` (hồ sơ); mật khẩu KHÔNG plaintext. `ServiceApiToken` trên `AppSettings` có `[JsonIgnore]` nên không serialize. (ASVS V2.10.1, V6.2.1)
- [x] **SEC-03**: Migration một lần: nếu JSON còn `ServiceApiToken` plaintext thì ghi kho, xóa khóa JSON, ghi lại file; dòng ghi `AppFileLog` category `credential_migration` (thực thi khi phát hiện legacy). (ASVS V2.10.4)
- [x] **SEC-04**: `POST /api/auth/rotate` (body `current_token`, envelope `data.new_token`); cập nhật `MutableToken` trong process; `golang.org/x/time/rate` 5 lần/phút/IP. (ASVS V2.5.3, V3.3.1, V13.1.5)
- [x] **SEC-05**: Settings có nút xoay token, dòng trạng thái `ServiceApiTokenCredentialStatus` mô tả nơi lưu. (ASVS V2.10.3)

### Abuse Prevention & Audit (AUD)

- [x] **AUD-01**: Per-IP `PerIPRateLimit`: REST 30/s burst 60, WebSocket upgrade 2/s burst 4; 429 + `Retry-After` (giây). (ASVS V11.1.4, V13.2.5)
- [x] **AUD-02**: `ReadTimeout`/`WriteTimeout` server 60s; `ExtendLongLivedRequestDeadlines` nới 30 phút cho whitelist (compose up/down/service logs, images load/pull/pull/stream, events stream, trivy, tiền tố `/ws/`, suffix container `.../logs|stats/stream`). (ASVS V11.1.1, V11.1.4)
- [x] **AUD-03**: `AuditSecuritySensitive`: JSON một dòng ra stdout + file cho endpoint nhạy cảm (prune, compose up/down, images load/pull/pull/stream, remove, prune, auth rotate); trường theo spec; không log token. (ASVS V7.1.1, V7.1.3, V8.3.4)
- [x] **AUD-04**: File `~/.docklite/logs/audit.log` xoay khi >10MB, xóa tệp `audit*` cũ hơn 14 ngày; stdout luôn có dòng audit. (ASVS V7.1.4, V7.2.1)

### TLS Opt-in với TOFU (TLS)

- [x] **TLS-01**: Khi `DOCKLITE_TLS_ENABLED=true`, service Go tự sinh self-signed cert ECDSA P-256 lần đầu; lưu `~/.docklite/tls/cert.pem` và `key.pem` với `chmod 0600`. Nếu file tồn tại, reuse. (ASVS V9.1.1, V9.2.1, V6.2.6)
- [x] **TLS-02**: Cert tự sinh có `CN = "DockLite WSL Service"`, `SAN = 127.0.0.1, ::1, <WSL IP>, <hostname>`, hiệu lực 10 năm, key usage `digitalSignature,keyEncipherment`, EKU `serverAuth`. (ASVS V9.2.2, V9.2.3)
- [x] **TLS-03**: WPF trust-on-first-use — lần đầu connect TLS tới một fingerprint chưa pin, hiển thị dialog với fingerprint SHA-256 (format `XX:XX:...`), subject, validity dates; user chọn "Trust" hoặc "Reject". Reject đóng connection, không lưu. (ASVS V9.2.4)
- [x] **TLS-04**: Fingerprint pin lưu trong Credential Manager cùng vault với token (target `DockLite:TrustedFingerprint:{host}:{port}`). (ASVS V2.10.4, V9.2.4)
- [x] **TLS-05**: Khi fingerprint đổi ở lần connect sau, WPF hiển thị dialog "Certificate changed — possibly MITM" với fingerprint cũ và mới; user chọn "Trust new" hoặc "Abort". Abort không kết nối. (ASVS V9.2.4, V14.4.5)
- [x] **TLS-06**: UI Settings có toggle "Enable TLS" và ô hiển thị fingerprint đang trust cho host hiện tại; nút "Forget pinned fingerprint" để reset trust. (ASVS V9.1.1)

### Process Hardening & Input Sanitization (PRC)

- [x] **PRC-01**: `WslDockerServiceAutoStart.SpawnWslLifecycleScript` không còn dùng `bash -lc "cd '...' && ./script"`. Chuyển sang `wsl.exe -d <distro> --cd <unixPath> -- bash <script>` (native argv, không có shell quoting) HOẶC giữ `bash -lc` nhưng quote path qua helper `BashSingleQuoted` và reject path chứa single-quote. (ASVS V5.3.8, V10.2.3)
- [x] **PRC-02**: Validation `wslUnixPath` reject các ký tự: `'`, `"`, backtick `` ` ``, `$`, `\n`, `\r`, `;`, `|`, `&`, `<`, `>`. Violation throw `ArgumentException` với message nêu ký tự vi phạm. (ASVS V5.1.5, V5.2.5)
- [x] **PRC-03**: Lifecycle script `run-server.sh`, `stop-server.sh`, `restart-server.sh` dùng PID file (`~/.docklite/run/docklite-wsl.pid`) thay cho `pkill -f docklite-wsl`. Stop script đọc PID, `kill -TERM $pid`; timeout 5s rồi `kill -KILL`. (Operational safety — không có ASVS cụ thể, thuộc V14.2.5 hardening process)
- [x] **PRC-04**: Unit test `WslDockerServiceAutoStartTests` thêm case inject: path chứa `'`, path chứa `$(rm -rf)`, path có newline. Expected: throw hoặc escaped đúng, KHÔNG execute injected command. (ASVS V5.3.8, Testing)
- [x] **PRC-05**: Unit test argument injection cho `ImageTrivyScan` (leading `-` trong `imageRef` phải reject) và `composeServiceExec` (service name bắt đầu bằng `-` phải reject, profile name chứa `;` phải reject). Mỗi handler nhận một suite test ít nhất 3 malicious input. (ASVS V5.3.8, V12.3.4)
- [x] **PRC-06**: Mọi lệnh spawn `docker compose`, `trivy`, `docker` CLI (trong Go và WPF) bắt buộc có `--` separator trước user-controlled positional arg. (ASVS V5.3.8)
- [x] **PRC-07**: `SECURITY-ATTESTATION.md` cuối milestone liệt kê toàn bộ requirement v2.0 với self-attest COVERED / PARTIAL / N/A + evidence (file + line range + test name). (ASVS V1.1.2, V1.1.6)

## v2.1 Requirements (Deferred — Pipe Channel)

Đã acknowledge nhưng không có trong roadmap v2.0. Khi kích hoạt v2.1, chuyển sang Active trong `REQUIREMENTS.md` mới (sau `/gsd-new-milestone`).

### Local Channel (LCH)

- **LCH-01**: Service Go hỗ trợ lắng nghe trên Windows named pipe `\\.\pipe\docklite-wsl` hoặc Unix socket chuyển tiếp qua WSL interop
- **LCH-02**: WPF client chọn kênh (pipe vs TCP) qua setting; pipe là mặc định mới
- **LCH-03**: Pipe mode không cần token (trust qua ACL pipe đúng user Windows)
- **LCH-04**: TCP mode giữ nguyên cứng hóa từ v2.0

## Out of Scope

Những feature đã xem xét và loại rõ. Ghi lại để chống scope creep.

| Feature | Lý do |
|---------|-------|
| mTLS client certificate | TOFU fingerprint đã đủ cho threat model; mTLS phức tạp cho end-user dev (phát/rotate cert client). Reassess khi có team dùng DockLite shared |
| Mã hóa toàn bộ `settings.json` | Chỉ token là sensitive; mã hóa toàn bộ đẻ ra UX master password không cần thiết |
| Quyền phân cấp read-only / admin token | Giữ single-role token trong v2.0; reassess sau khi có audit log thực tế |
| Gửi log sang SIEM / OpenTelemetry | DockLite là local dev tool; file log đủ cho forensic cá nhân |
| SLSA L3 / CI supply-chain hardening | Tách milestone riêng "CI/CD Hardening" sau; v2.0 không động `.github/workflows/` |
| Tự động ZAP / govulncheck / semgrep trong CI | Self-attest thủ công đủ cho v2.0; automation sang milestone sau |
| CIS Docker Benchmark mapping chính thức | Chọn ASVS L2 làm chuẩn chính để gọn; CIS Docker chỉ tham chiếu gián tiếp trong docs |
| CWE Top 25 tagging từng finding | Overhead không cần khi đã map ASVS |
| Microsoft SDL full process (threat modeling, fuzz) | Quá nặng cho 1 milestone; áp dụng rải rác nhưng không commit full process |
| Pipe / named-pipe channel trong v2.0 | Cần research thêm transport WPF↔WSL2 (vsock vs named pipe vs socat). Chuyển sang v2.1 |
| Đổi HTTP framework (ví dụ sang `chi`, `echo`, `fiber`) | Stack `net/http` + `gorilla/mux` đã ổn; đổi framework là yak shave |
| Rewrite WPF sang WinUI 3 | Không liên quan bảo mật; dự án riêng, không trong v2.0 |

## Traceability

Mapping requirement ↔ phase. Trạng thái cuối milestone v2.0.

| Requirement | Phase | Status |
|-------------|-------|--------|
| NET-01 | Phase 1 | Complete |
| NET-02 | Phase 1 | Complete |
| NET-03 | Phase 1 | Complete |
| NET-04 | Phase 1 | Complete |
| NET-05 | Phase 1 | Complete |
| SEC-01 | Phase 2 | Complete |
| SEC-02 | Phase 2 | Complete |
| SEC-03 | Phase 2 | Complete |
| SEC-04 | Phase 2 | Complete |
| SEC-05 | Phase 2 | Complete |
| AUD-01 | Phase 3 | Complete |
| AUD-02 | Phase 3 | Complete |
| AUD-03 | Phase 3 | Complete |
| AUD-04 | Phase 3 | Complete |
| TLS-01 | Phase 4 | Complete |
| TLS-02 | Phase 4 | Complete |
| TLS-03 | Phase 4 | Complete |
| TLS-04 | Phase 4 | Complete |
| TLS-05 | Phase 4 | Complete |
| TLS-06 | Phase 4 | Complete |
| PRC-01 | Phase 5 | Complete |
| PRC-02 | Phase 5 | Complete |
| PRC-03 | Phase 5 | Complete |
| PRC-04 | Phase 5 | Complete |
| PRC-05 | Phase 5 | Complete |
| PRC-06 | Phase 5 | Complete |
| PRC-07 | Phase 5 | Complete |

**Coverage:**
- v2.0 requirements: 27 total
- Mapped to phases: 27
- Unmapped: 0

---
*Requirements defined: 2026-04-23*  
*Archived: 2026-04-25 (đóng milestone v2.0)*
